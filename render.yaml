services:
  - type: web
    name: ZClassScheduler
    plan: free           # or the plan you prefer
    runtime: java        # ensures Render installs a JDK
    buildCommand: ./gradlew build
    startCommand: java -jar build/libs/ZClassScheduler-all.jar